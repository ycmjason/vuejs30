<template>
  <div class="panel" :class="opened? 'opened': ''" :style="panelStyle" @click="opened = !opened">
    <p>{{ panel.words[0] }}</p>
    <p>{{ panel.words[1] }}</p>
    <p>{{ panel.words[2] }}</p>
  </div>
</template>

<script>
export default {
  props: {
    panel: {
      type: Object,
      required: true,
    },
  },
  data: () => ({
    opened: false,
  }),
  computed: {
    panelStyle () {
      return {
        backgroundImage: `url(${this.panel.image})`,
      };
    },
  },
}
</script>

<style scoped>
.panel {
  background: #6B0F9C;
  box-shadow: inset 0 0 0 5px rgba(255,255,255,0.1);
  color: white;
  text-align: center;
  align-items: center;
  transition:
    font-size 0.7s cubic-bezier(0.61,-0.19, 0.7,-0.11),
    flex 0.7s cubic-bezier(0.61,-0.19, 0.7,-0.11),
    background 0.2s;
  font-size: 20px;
  background-size: cover;
  background-position: center;
  flex: 1;
  justify-content: center;
  display: flex;
  flex-direction: column;
}

.panel > * {
  margin: 0;
  width: 100%;
  transition: transform 0.5s 0.7s;
  flex: 1 0 auto;
  display: flex;
  justify-content: center;
  align-items: center;
}

.panel > *:first-child {
  transform: translateY(-100%);
}

.panel.opened > *:first-child {
  transform: translateY(0);
}

.panel > *:last-child {
  transform: translateY(100%);
}

.panel.opened > *:last-child {
  transform: translateY(0);
}

.panel p {
  text-transform: uppercase;
  font-family: 'Amatic SC', cursive;
  text-shadow: 0 0 4px rgba(0, 0, 0, 0.72), 0 0 14px rgba(0, 0, 0, 0.45);
  font-size: 2em;
}
.panel p:nth-child(2) {
  font-size: 4em;
}

.panel.opened {
  flex: 5;
  font-size: 40px;
}
</style>
